<%= @current_user.inspect %>
<form class="form-horizontal" role="form">
  <div class = "container-fluid">
    <div class = "col-md-4"></div>
      <div class = "col-md-4">
         <h1 class="text-center" id = "ultimate-orange">Guest List</h1>
      </div>
        <div class = "col-md-4"></div>
 </div>

  <% @guests.each do |guest| %>
  <div class="row">
      <h4  id = "ultimate-orange-smaller"><div class="col-sm-1"></div><div class="col-sm-1"></div><div class="col-sm-1"></div><div class="col-sm-4"><%= link_to guest.mailing_title, edit_guest_path(guest)%></div><div class="col-sm-4"><%= link_to "Input Place Cards", guest_place_cards_path(guest)%></div></h4>
  </div>
  <% end %>
</form>
<nav>
   <h2 id = "ultimate-orange"><span class="pull-left"><%= link_to "Add Guest", new_guest_path %></span><span class="pull-right"><%= link_to "Sign Out", session_path, method: :delete %></span></h2>
</nav>
